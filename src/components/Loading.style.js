import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    position: 'absolute',
    backgroundColor: 'white',
    alignSlef: 'center',
    padding: 12,
    borderRadius: 10,
    borderColor: '#e0e0e0e',
    alignSelf: 'center',
    bottom: '50%',
  },
});
