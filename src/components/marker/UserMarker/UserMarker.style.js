const {StyleSheet} = require('react-native');

export default StyleSheet.create({
  image: {
    width: 50,
    height: 50,
    borderRadius: 25,
    borderColor: '#e0e0e0',
    backgroundColor: 'white',
  },
});
